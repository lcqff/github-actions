name: tag-filter

# push에서만 동작한다.
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+' # v{x}.{x}.{x} 형태로 태그 시 트리거

jobs:
  tag-filter:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo hello world


# git tag v0.0.0
# git push origin v0.0.0