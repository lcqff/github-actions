name: multiple-trigger-workflow
on:
  push:
  issues:
    types:
      - opened
  workflow_dispatch:

jobs:
  multiple-trigger-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo hello world
      - name: multiLine
        run: |
          echo hello world
          echo github action